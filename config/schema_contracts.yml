yellow:
  required_fields:
    pickup_datetime:
      aliases: [tpep_pickup_datetime]
      accepted_types: [timestamp]
    dropoff_datetime:
      aliases: [tpep_dropoff_datetime]
      accepted_types: [timestamp]
    pickup_location_id:
      aliases: [PULocationID, pulocationid]
      accepted_types: [byte, short, integer, long]
    dropoff_location_id:
      aliases: [DOLocationID, dolocationid]
      accepted_types: [byte, short, integer, long]
  optional_fields:
    vendor_id: {aliases: [VendorID, vendorid]}
    passenger_count: {aliases: [passenger_count]}
    trip_distance: {aliases: [trip_distance]}
    rate_code_id: {aliases: [RatecodeID, ratecodeid]}
    store_and_fwd_flag: {aliases: [store_and_fwd_flag]}
    payment_type: {aliases: [payment_type]}
    fare_amount: {aliases: [fare_amount]}
    extra: {aliases: [extra]}
    mta_tax: {aliases: [mta_tax]}
    tip_amount: {aliases: [tip_amount]}
    tolls_amount: {aliases: [tolls_amount]}
    improvement_surcharge: {aliases: [improvement_surcharge]}
    total_amount: {aliases: [total_amount]}
    congestion_surcharge: {aliases: [congestion_surcharge]}
    airport_fee: {aliases: [Airport_fee, airport_fee]}
    cbd_congestion_fee: {aliases: [cbd_congestion_fee]}

green:
  required_fields:
    pickup_datetime:
      aliases: [lpep_pickup_datetime]
      accepted_types: [timestamp]
    dropoff_datetime:
      aliases: [lpep_dropoff_datetime]
      accepted_types: [timestamp]
    pickup_location_id:
      aliases: [PULocationID, pulocationid]
      accepted_types: [byte, short, integer, long]
    dropoff_location_id:
      aliases: [DOLocationID, dolocationid]
      accepted_types: [byte, short, integer, long]
  optional_fields:
    vendor_id: {aliases: [VendorID, vendorid]}
    store_and_fwd_flag: {aliases: [store_and_fwd_flag]}
    rate_code_id: {aliases: [RatecodeID, ratecodeid]}
    passenger_count: {aliases: [passenger_count]}
    trip_distance: {aliases: [trip_distance]}
    fare_amount: {aliases: [fare_amount]}
    extra: {aliases: [extra]}
    mta_tax: {aliases: [mta_tax]}
    tip_amount: {aliases: [tip_amount]}
    tolls_amount: {aliases: [tolls_amount]}
    improvement_surcharge: {aliases: [improvement_surcharge]}
    total_amount: {aliases: [total_amount]}
    payment_type: {aliases: [payment_type]}
    trip_type: {aliases: [trip_type]}
    congestion_surcharge: {aliases: [congestion_surcharge]}
    cbd_congestion_fee: {aliases: [cbd_congestion_fee]}
    ehail_fee: {aliases: [ehail_fee]}

fhv:
  required_fields:
    dispatching_base_num:
      aliases: [dispatching_base_num]
      accepted_types: [string]
    pickup_datetime:
      aliases: [pickup_datetime]
      accepted_types: [timestamp]
    dropoff_datetime:
      aliases: [dropOff_datetime, dropoff_datetime]
      accepted_types: [timestamp]
    pickup_location_id:
      aliases: [PUlocationID, PULocationID, pulocationid]
      accepted_types: [byte, short, integer, long]
    dropoff_location_id:
      aliases: [DOlocationID, DOLocationID, dolocationid]
      accepted_types: [byte, short, integer, long]
  optional_fields:
    shared_ride_flag: {aliases: [SR_Flag, sr_flag]}
    affiliated_base_number: {aliases: [Affiliated_base_number, affiliated_base_number]}

fhvhv:
  required_fields:
    hvfhs_license_num:
      aliases: [hvfhs_license_num]
      accepted_types: [string]
    dispatching_base_num:
      aliases: [dispatching_base_num]
      accepted_types: [string]
    request_datetime:
      aliases: [request_datetime]
      accepted_types: [timestamp]
    pickup_datetime:
      aliases: [pickup_datetime]
      accepted_types: [timestamp]
    dropoff_datetime:
      aliases: [dropoff_datetime]
      accepted_types: [timestamp]
    pickup_location_id:
      aliases: [PULocationID, pulocationid]
      accepted_types: [byte, short, integer, long]
    dropoff_location_id:
      aliases: [DOLocationID, dolocationid]
      accepted_types: [byte, short, integer, long]
  optional_fields:
    originating_base_num: {aliases: [originating_base_num]}
    on_scene_datetime: {aliases: [on_scene_datetime]}
    trip_miles: {aliases: [trip_miles]}
    trip_time: {aliases: [trip_time]}
    base_passenger_fare: {aliases: [base_passenger_fare]}
    tolls: {aliases: [tolls]}
    black_car_fund: {aliases: [bcf]}
    sales_tax: {aliases: [sales_tax]}
    congestion_surcharge: {aliases: [congestion_surcharge]}
    airport_fee: {aliases: [airport_fee]}
    tips: {aliases: [tips]}
    driver_pay: {aliases: [driver_pay]}
    shared_request_flag: {aliases: [shared_request_flag]}
    shared_match_flag: {aliases: [shared_match_flag]}
    access_a_ride_flag: {aliases: [access_a_ride_flag]}
    wav_request_flag: {aliases: [wav_request_flag]}
    wav_match_flag: {aliases: [wav_match_flag]}
    cbd_congestion_fee: {aliases: [cbd_congestion_fee]}
